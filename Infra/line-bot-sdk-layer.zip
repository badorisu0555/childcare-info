mkdir -p line-bot-sdk-layer/python
pip install line-bot-sdk \
  --platform manylinux2014_x86_64 \
  --target line-bot-sdk-layer/python \
  --python-version 3.12 \
  --only-binary=:all:

cd line-bot-sdk-layer
zip -r ../line-bot-sdk-layer.zip python
cd ..